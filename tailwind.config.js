import { trueGray, red as _red, lightBlue, indigo } from 'tailwindcss/colors';
export const purge = [];
export const darkMode = false;

export const theme = {
    colors: {
        // Build your palette here
        transparent: 'transparent',
        current: 'currentColor',
        gray: trueGray,
        red: _red,
        blue: lightBlue,
        yellow: colors.amber
    }
};

export const variants = {
    extend: {}
};
export const plugins = [];
